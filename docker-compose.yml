version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:///dueltech.db
      - FLASK_ENV=production
      - FLASK_DEBUG=0
    restart: always
